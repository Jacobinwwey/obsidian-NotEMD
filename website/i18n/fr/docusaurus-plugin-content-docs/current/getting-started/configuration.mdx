---
id: configuration
title: Configuration
description: Explique le rôle de Configuration dans Notemd, sa configuration, ses limites de workflow et les vérifications courantes.
author:
  '@id': 'https://jacobinwwey.github.io/obsidian-NotEMD/#person-jacobinwwey'
---

import TLDR from '@site/src/components/TLDR';

# Configuration

<TLDR>
**Résumé：** Explique le rôle de Configuration dans Notemd, sa configuration, ses limites de workflow et les vérifications courantes. Les commandes, clés de configuration, extensions et noms de Provider ci-dessous restent en anglais afin de correspondre exactement à l’interface, au CLI et aux journaux.
</TLDR>

## Configuration : configuration et modèles

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : explication principale

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## 1. Configuration du Provider

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

#### OpenAI

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

#### Configuration : explication principale

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

#### Configuration : limites et gouvernance

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

#### DeepSeek

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## 2. Modèles par tâche

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : workflows et tâches

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : installation et préparation

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## 3. Configuration de sortie

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : workflows et tâches

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## 4. Paramètres de langue

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : traduction et langues

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## 5. Configuration : recherche

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : recherche

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : recherche

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## 6. Configuration des workflows

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : workflows et tâches

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : explication principale

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## 7. Paramètres avancés

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : workflows et tâches

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : explication principale

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : explication principale

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : explication principale

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## Configuration : configuration et modèles

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : configuration et modèles

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

## Configuration : configuration et modèles

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : explication principale

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : recherche

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : limites et gouvernance

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : traduction et langues

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## Configuration : configuration et modèles

Cette section sert à diagnostiquer les échecs courants liés à Configuration. Vérifiez d’abord les diagnostics Notemd, le statut HTTP du Provider, le nom du modèle, le réseau et les limites de débit.

### Configuration : configuration et modèles

La configuration de Configuration se fait dans les paramètres Notemd. Provider, modèle, sortie, langue et traitement par lots interagissent; identifiez si le changement touche le fichier courant, un dossier ou un workflow.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

### Configuration : configuration et modèles

Cette partie de Configuration décrit le comportement produit, les points d’entrée et les limites d’implémentation. Les boutons, commandes, clés et extensions restent des contrats stables.

## Étapes suivantes

Pour continuer, choisissez la page qui correspond directement à votre chaîne de travail: Provider, fonction principale, traitement par lots, dépannage ou export de diagrammes.
