// Compatibility import path for render-host consumers. The canonical Mermaid
// normalization boundary lives in the diagram adapter layer.
export { normalizeMermaidDefinition } from '../../diagram/adapters/mermaid/normalize';
