# Delivery roadmap

Purpose: Use for dated milestones where sequence and timing are the primary meaning.

Requested diagram type: `timeline`
Requested render target: `mermaid`

## Source facts

- spec.timelineEvents[1]: discovery (Discovery)
- spec.timelineEvents[1].id: discovery
- spec.timelineEvents[1].date: 2024 Q1
- spec.timelineEvents[1].details: Define the contract
- spec.timelineEvents[2]: preview (Preview)
- spec.timelineEvents[2].id: preview
- spec.timelineEvents[2].date: 2024 Q2
- spec.timelineEvents[2].details: Ship deterministic examples
- spec.timelineEvents[3]: release (Release)
- spec.timelineEvents[3].id: release
- spec.timelineEvents[3].date: 2024 Q3

## Reading cues

- Confirm that the timeline output preserves the source facts and relationships.
- Check that the visual structure matches the declared mermaid render target.
- Inspect this evidence first: spec.timelineEvents[1]: discovery (Discovery)

This is an input example. The corresponding output is real-vault evidence with provider metadata recorded in `machine-test.json`.
