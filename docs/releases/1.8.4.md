# Notemd v1.8.4

## Highlights

- Release workflow hardening: GitHub Actions majors are now pinned to supported versions so the Node 20 deprecation path does not age into a release failure.
- Progress and roadmap docs were corrected to match current code and remote workflow truth.
- Drawnix was audited and documented as a reference boundary, not a near-term embedded host target.
- A new mainline stabilization and CI hardening requirements document was added to capture the next execution batch.

## New Features

- Maintainer-facing release workflow docs now explain how to read `main` truth correctly, including the `pending` commit-status / zero-status edge case.
- The repository now carries a dedicated `mainline stabilization and CI hardening` requirements document for the next batch of work.

## Fixes

- Updated roadmap, maintainer release workflow docs, progress audit docs, Drawnix audit docs, and phase-2 requirements references so they reflect current reality.
- Bumped `package.json`, `manifest.json`, `versions.json`, and the README version banners to `1.8.4`.

## Chores

- Added bilingual release notes for `1.8.4`.
- Kept the existing `1.8.3` release notes as historical context.
