# Notemd v1.9.7

发布日期：2026-08-30

## 重点更新

- 修复 Mermaid 象限图标签错误显示坐标数组结构性 `[` 的问题。
- 修复 editable HTML/SVG lane-grid 图中摘要、标题、单元格、连接线和边标签互相覆盖的问题。
- 新增碰撞感知的正交路由和受保护的标题间距，长文本无需用户手动横向滚动即可保持可读。
- 在 `docs/diagram-examples/` 下补齐真实 Vault 示例集，包含双语 Markdown 输入、实测生成的 SVG/PNG 结果和机器可读 manifest。
- 更新仓库文档、维护者手册和 GitHub Pages 图表指南，记录修复契约与实机证据。
- 参考 SimRepo 的格式，将所有 README 语言版本中已停用的 Star History 嵌入替换为无需 token 的 `star-history.dera.page` 响应式图表。

## 根因与修复

象限图问题的根因是：旧版 flowchart 引号/形状修复链被应用到了 `quadrantChart` 代码块。合法点位 `"Docs gallery": [0.32, 0.68]` 可能被改写成带可见括号的标签。现在会先按 Mermaid family 做门控，专用规范化器只会从象限数据点标签末尾移除不匹配的结构性括号。

lane-grid 问题的根因是：摘要高度和占用单元格尚未测量时就固定放置标题与标签。现在先测量并换行摘要，再把标题放到受保护区域下方；随后先绘制单元格，再绘制连接线和标签；跨 lane 的边使用碰撞感知的正交网格路径。

## 验证

- Mermaid 与布局聚焦测试：55 项全部通过。
- 发布候选版本会重新执行完整 Jest、TypeScript 与构建门禁。
- 重新加载更新后的 bundle 后，在真实 `1Knowledge` Vault 中成功生成干净的 `quadrant` 与 `data-flow` 产物。
- 发布包包含必需的 `main.js`、`manifest.json`、`styles.css` 和 `README.md` 资产。

## 1.9.6 至 1.9.7 提交摘要

本版本包含 `1.9.6` 标签之后、最终 `1.9.7` 发布说明元数据提交之前的全部 49 个工程提交。为便于逐条追溯，提交主题保留仓库原文：

- `6a8da7f` docs: refresh quarterly chronicle for 1.9.6
- `6c7f395` feat(docs): harden multilingual localization pipeline
- `33c7097` feat(drawnix): preserve complex knowledge-map topology
- `a802e6a` feat(drawnix): benchmark complex knowledge-map exports
- `7a5fb47` docs(drawnix): record presentation architecture review
- `4168a51` feat(drawnix): add replayable presentation delivery
- `ce5dc43` docs(mermaid): plan normalization consolidation and sync roadmap progress
- `67fcf69` fix(drawnix): restore native filename-rooted tree
- `5efd428` docs(diagrams): define capability catalog and forward architecture
- `749ace5` feat(diagrams): add capability catalog and deterministic previews
- `fc627af` fix(cache): keep response fingerprint portable
- `f193406` docs(diagrams): refresh verification counts
- `99839f6` feat: converge diagram contracts and Mermaid normalization
- `426d0e8` feat: stage and gate Mermaid legacy repairs
- `cdf580c` fix(mermaid): classify known families before legacy repair
- `eb67d43` feat: converge diagram capability contracts
- `132858b` feat: converge Drawnix and Circuitikz adapters
- `b4f4f14` refactor(drawnix): centralize text layout geometry
- `a187749` docs(diagram): record target dispatch debt
- `f3a141d` Converge render target adapter dispatch
- `8c9efe7` feat: expand diagram catalog and presentation contracts
- `280be84` fix: remove unused webview presentation export
- `6097ff1` docs: sync diagram roadmap closure status
- `89ba384` feat: add Drawnix consumer gate and geometry boundary
- `f46393f` fix: harden operation contract admission
- `5ccdf45` fix: reject incomplete required schemas
- `f531743` docs: record contract admission boundary
- `106112c` feat(diagrams): ship radar capability catalog
- `0e362a4` feat(diagrams): add bounded org chart capability
- `22737ec` refactor(diagrams): converge label and port geometry
- `948d1e9` feat(diagrams): expose reference previews in type picker
- `07c629c` feat(diagrams): render selected production previews
- `b0f9f4e` fix(diagrams): sync settings target selector
- `610d53f` feat(diagrams): establish forward-compatible expansion boundary
- `188f4dd` fix(diagrams): keep default variant planning explicit
- `b16d51d` feat(diagrams): ship reference layout payload families
- `84334c2` docs(diagrams): correct expansion progress counts
- `7c1b104` feat(diagrams): expose variant-aware catalog selections
- `7f9b482` fix(diagrams): enforce layout safety across generated previews
- `a604293` fix(diagrams): preserve readable native previews
- `e0d5986` fix(diagrams): close root-layer preview occlusion
- `81a60e3` fix(diagrams): promote canvas preview safety
- `563baf3` fix(diagrams): reject unsafe canvas artifacts before save
- `e4ca117` fix(diagrams): close preview text occlusion paths
- `5d9f6bd` fix(diagrams): enforce cross-family presentation safety
- `a8512fb` docs(diagrams): define real-vault examples evidence
- `8974d0f` docs(diagrams): add real-vault examples implementation plan
- `43bbe8e` fix(diagrams): ship readable previews and 1.9.7 docs
- `a696cd6` fix(release): make diagram examples bilingual and complete 1.9.7 notes

## 文档与翻译

英文与简体中文的发布说明和文档由 Codex 同步撰写并复核。本版本不需要 LM Studio 或任何外部翻译 API；各语言文件通过离线结构、链接和技术 token 校验。
