# Notemd v1.8.4

## 亮点

- release workflow 已加固：GitHub Actions 的 major 版本固定到受支持范围，避免 Node 20 弃用路径最终演变成发布失败。
- 进度文档与路线图已校正到当前代码和远端 workflow 真相。
- Drawnix 已被审计并明确为参考边界，而不是近期要嵌入的宿主。
- 新增主线稳定化与 CI 加固需求文档，用来承接下一批执行工作。

## 新功能

- 维护者发布流程文档现在会明确说明如何读取 `main` 的真实状态，包括 `pending` commit-status / 零 status 这一特殊情况。
- 仓库新增专门的 `mainline stabilization and CI hardening` 需求文档，承接下一批工作。

## 修复

- 已更新路线图、维护者发布流程文档、进展审计文档、Drawnix 审计文档以及 phase-2 需求引用，使其与当前事实一致。
- `package.json`、`manifest.json`、`versions.json` 与 README 的版本标识已同步到 `1.8.4`。

## 杂项

- 新增 `1.8.4` 的双语发布说明。
- 保留 `1.8.3` 的历史发布说明作为上下文。
